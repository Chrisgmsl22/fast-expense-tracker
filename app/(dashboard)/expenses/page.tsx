// Expenses page shell — capture modal (1.4) + list/month filter (1.5) land later.
export default function ExpensesPage() {
    return (
        <main className="p-8">
            <h1 className="text-2xl font-semibold">Expenses</h1>
        </main>
    );
}
