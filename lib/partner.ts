/**
 * The partner's display name, shown across the money-movement UI — the Add menu
 * ("I paid {name}"), feed lines, footer ("Paid to {name}"), and the reminder
 * ("{name} owes you"). Single source (ADR-0018); if this ever becomes
 * user-configurable it moves to Settings and this constant becomes the default.
 */
export const PARTNER_NAME = "Brenda";
